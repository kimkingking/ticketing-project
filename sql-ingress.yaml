apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: internal-http-ingress  # 💡 중요: 팀원 파일 이름과 똑같이 맞춰야 에러가 안 남!
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    
    # [1. 님의 WAF 보안 설정]
    nginx.ingress.kubernetes.io/enable-modsecurity: "true"
    nginx.ingress.kubernetes.io/enable-owasp-core-rules: "true"
    nginx.ingress.kubernetes.io/modsecurity-snippet: |
      SecRuleEngine On
      SecAuditLog /dev/stdout
      SecAuditLogType Serial

    # [2. 님의 DDoS 방어 설정]
    nginx.ingress.kubernetes.io/limit-rps: "5"           # 초당 5번만 허용 (테스트용으로 직빵)
    nginx.ingress.kubernetes.io/limit-connections: "3"    # 동시에 3개 연결만 허용

    # [3. 팀원 파일에 있던 기존 설정들]
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/enable-cors: "true"

spec:
  ingressClassName: nginx
  rules:
  - http: # IP 접속용 (10.4.0.203)
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service: { name: ticket-backend-svc, port: { number: 8000 } }
      - path: /
        pathType: Prefix
        backend:
          service: { name: ticket-frontend-svc, port: { number: 80 } }
